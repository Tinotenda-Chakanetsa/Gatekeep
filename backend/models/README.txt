Place your local Ultralytics YOLO license plate detector weights in this folder.

Expected default filename:
license_plate_detector.pt

You can also keep the weights anywhere else and set the YOLO_MODEL_PATH environment variable.
